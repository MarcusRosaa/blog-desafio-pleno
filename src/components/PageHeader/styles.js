import styled from 'styled-components';

export const Container = styled.header`
  a {
    font-size: 16px;
    font-weight: bold;
    display: flex;
    align-items: center;
    margin-bottom: 8px;

    img {
      margin-right: 8px;
      transform: rotate(-90deg);
    }
  }

  h1 {
    margin-bottom: 16px;
  }

`;
